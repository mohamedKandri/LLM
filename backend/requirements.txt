# Core (phase 1 pipeline)
fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.32
pyyaml>=6.0
pytest>=8.0
httpx>=0.27         # FastAPI TestClient (tests/test_main.py)

# Training (trainer.py) — CPU-only on this dev machine (no discrete
# GPU), so plain transformers+peft+trl, not Unsloth (requires CUDA).
# Install the CPU build of torch first: pip install torch --index-url
# https://download.pytorch.org/whl/cpu
torch
transformers>=4.46
peft>=0.13
trl>=0.12
datasets

# sentence-transformers   # optional: swap in for dataset_manager's
                          # dedup embedder if the hashed-trigram default
                          # isn't precise enough later
